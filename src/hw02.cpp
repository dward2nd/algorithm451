#include <cstdint>
#include <iostream>
#include <utility>

std::uint_fast8_t max_events_possible(std::uint_fast8_t &n,
                                      std::pair<int, int> *events) {}

void hw02() {
    std::uint_fast8_t T;
    std::cin >> T;

    while (T--) {
        std::uint_fast8_t n;
        std::cin >> n;

        // std::cout << max_events_possible(n, );
    }
}
